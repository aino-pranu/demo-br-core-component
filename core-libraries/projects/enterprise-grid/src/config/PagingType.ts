/**
 * PagingType class to configure two types of pagination : scroll and paging(with next/prev buttons).
 */
export class PagingTypes {

    //scroll pagination
    static readonly scroll: string = "scroll";

    //footer toolbar pagination
    static readonly paging: string = "paging";
}